package org.lengine.tests

import org.lengine.entity.Entity

class EntityPlayer extends Entity {
  override def render(delta: Float): Unit = {

  }

  override def init: Unit = {

  }

  override def update(delta: Float): Unit = {

  }
}
